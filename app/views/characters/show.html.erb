<h1>登場人物詳細</h1>
  <h3><%= @story.title %></h3>
  <p><%= @character.name %></p>
  <p><%= @character.gender %></p>
  
  <%= link_to("編集",edit_story_character_path(@story,@character)) %>
  <%= link_to("back",story_path(@story)) %>