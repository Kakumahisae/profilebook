<h3><%= @story.title %></h3>

<h1>登場人物一覧</h1>
  <% if @characters %>
    <% @characters.each do |character| %>
      <ul>
          <li><%= character.name %></li>
      </ul>
    <% end %>
  <% end %>    


<%= link_to("新規登場人物", new_story_character_path) %>